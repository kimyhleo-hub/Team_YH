Review the current assignment or workflow changes against these repository rules:

1. Does the output match the requested deliverables?
2. Are shared rules still in shared docs and assignment-specific rules still local?
3. Are unverified MATLAB or Python results clearly marked?
4. Does `solution.md` include formulas, units, assumptions, verification status, and `**정답:**`?
5. Are graphs labeled and interpreted separately from presentation?
6. If workflow logic changed, should `AGENT.md`, `Plan.md`, `MATLAB_WORKFLOW.md`, or assignment `notes.md` be updated?

List findings first. If there are no findings, say so explicitly and note residual risks.
